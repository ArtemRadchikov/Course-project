<Window x:Class="Helper.View.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Helper"
        
        mc:Ignorable="d"
        Title="MainWindow" Height="768" Width="1349" WindowStyle="None"  WindowState="Maximized" ResizeMode = "NoResize">
    <Grid>
        <Frame Content="{Binding CurrentPage}" NavigationUIVisibility="Hidden" />
        <Button HorizontalAlignment="Right" VerticalAlignment="Top" Height="60" Width="60" Content="{StaticResource ExetIcon}" Style="{StaticResource BaseButton}" Command="{Binding ExetFromApp}" />
    </Grid>
</Window>
